{{- if .Values.k8sAuth.enabled }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.k8sAuth.syncServiceAccountName | default "aeterna-sync" }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "aeterna.labels" . | nindent 4 }}
{{- end }}
