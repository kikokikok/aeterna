## ADDED Requirements

### Requirement: Memory Reason Tool
The tool interface SHALL provide a `memory_reason` tool to assist agents in planning their memory retrieval.

#### Scenario: Tool Invocation
- **WHEN** the `memory_reason` tool is called with a natural language query
- **THEN** it SHALL return a list of optimized search parameters and a rationale
