# Tasks — harden-tenant-provisioning

> **This file was rewritten on 2026-04-20 after a full audit of master**
> **(commit `fab6405`). See `AUDIT.md` for the evidence behind each status.**
>
> Legend:
> - `[ ]` = not started (MISSING in audit)
> - `[~]` = partially done (existing code needs extension; audit “PARTIAL”)
> - `[!]` = **design conflict must be resolved before coding** (audit “CONFLICTS”)
> - `[x]` = done

---

## 0. Resolve design conflicts (blocking all other groups)

- [x] 0.1 ~~Decide `SecretResolver` shape~~ **RESOLVED** in `design.md` §D9: **kind-dispatched trait** (`SecretRefResolver` + `SecretResolverRegistry`). Migrated incrementally via a `LegacyClosureAdapter` so 3.1 lands with zero runtime behaviour change; closure typedef deleted in 3.5. _(unblocks 3.*)_
- [x] 0.2 ~~Decide provider wiring model~~ **RESOLVED** in `design.md` §D5: **Eager** (boot loop + Dragonfly pub/sub `tenant:changed` fan-out + lazy fallback on registry miss). Failure policy per-tenant by default, strict mode opt-in. Acceptance: freshly provisioned tenant is usable cluster-wide with no pod restart; zero user-visible 500s on race windows.
- [!] 0.3 Define `TenantSecretReference` migration path: introduce `TenantSecretReferenceV2` sum type alongside the existing flat struct, with a deprecation window — or commit to a single breaking bump of `mk_core`. Document in `design.md`. _(blocks 1.2, 3.2-3.4)_
- [x] 0.4 ~~Decide CLI `validate` placement~~ **RESOLVED** in `design.md` §D8: **top-level `tenant validate` subsumes nested** `tenant repo-binding validate` and `tenant config validate`. Nested subcommands kept as deprecated aliases emitting a stderr warning and routing to the top-level code path; removed after two minor versions. _(unblocks 7.4, 7.6)_

---

## Phase B1 — Manifest schema + hashing foundation

### 1. Manifest schema and hashing

- [x] 1.1 Extend `TenantManifest` with `metadata.generation: u64` and `providers: ManifestProviders` (memoryLayers, llm, embedding). _(Shipped as part of the #94/#104 family: `ManifestMetadata { generation, labels, annotations }` and `ManifestProviders { llm, embedding, memory_layers }` at `cli/src/server/tenant_api.rs:3443-3525`; `ManifestProvider { kind, model, secret_ref, config }` with logical `secret_ref` into `config.secret_references`.)_
- [x] 1.2 Introduce `SecretReference` sum type per 0.3. Update `ManifestConfig.secret_references` accordingly. _(PR #127 — hard cut per design.md D1: full variant set shipped (`Inline`, `Postgres`, `Env`, `File`, `K8s`, `Vault`). Per-variant `validate_manifest` well-formedness checks; serde `tag="kind"` rejects unknown kinds at parse time (locked by dedicated test). `SecretBackend::{get,delete}` route non-Postgres variants to pre-existing `UnsupportedReference(kind)` until dispatching backends land. `SecretBytes`'s always-redact `Serialize` means `Inline` values cannot leak on reserialize; `expose_inline_plaintext()` is the only accessor, used solely on the write path. 10 mk_core tests + 9 validate_manifest tests added.)_
- [x] 1.3 Create `manifest_canonical` module: stable key order, inline secrets stripped. _(Shipped as `cli/src/server/manifest_hash.rs` by the #94/#104 family — `canonicalize()` emits lexicographic object keys via a recursive pass, `strip_plaintext` elides `secretValue`/`secret_value` fields before hashing. Not a separate `manifest_canonical` module — canonicalization lives with the hash since the two always travel together.)_
- [x] 1.4 Implement `manifest_hash(&TenantManifest) -> String` returning `sha256:<hex>`. _(`hash_manifest_value(&Value) -> Result<String, serde_json::Error>` at `cli/src/server/manifest_hash.rs:58`. 71-char output format `sha256:<64 hex>`. Takes `Value` (not `&TenantManifest` typed) on purpose so callers pass the raw wire shape and the hash stays stable across any typed-struct drift. 10 unit tests locking: prefix+length, byte-for-byte determinism, key-order stability, array-order preservation, plaintext stripping (both casings), generation sensitivity, empty-vs-missing distinction, known vector.)_
- [x] 1.5 Add `last_applied_manifest_hash: Option<String>` and `generation: u64` to the tenant record; write SQL migration. _(PR #105 — migration `027_tenant_manifest_state.sql`; `TenantStore::{get,set}_manifest_state` + `set_manifest_state_tx` with BYTEA↔hex helpers; 10 integration tests inc. idempotency, roundtrip, nonexistent-slug, tx rollback.)_
- [x] 1.6 Update `provision_tenant` (`tenant_api.rs:3599`) to compute+persist hash, enforce monotonic generation, short-circuit no-op re-applies. Rewrite `provision_tenant_idempotent_reapply` (L5674) to assert the short-circuit path. _(PR #121 — pre-mutation hash read+short-circuit returning 200 on unchanged manifest; post-commit `set_manifest_state_tx` under the same transaction as the tenant upsert; generation auto-increments; audit logs now carry `manifest_hash` and `generation`.)_
- [x] 1.7 Extend `validate_manifest` (called at L3609) to cover unknown `SecretReference.kind`, invalid generation, missing required provider declarations. _(Already shipped alongside 1.1: `validate_manifest_rejects_generation_zero` (tenant_api.rs:6132), `validate_manifest_rejects_unresolved_provider_secret_ref` (6207), `validate_manifest_rejects_provider_empty_kind` (6227) all green. "Unknown `SecretReference.kind`" case is structurally impossible today because serde's `#[serde(tag="kind")]` on the `SecretReference` enum rejects unknown tags at deserialization — the validator never sees a `SecretReference` it does not understand. Will need an explicit `UnknownKind` variant + rejection test once additional variants ship under task 1.2 / 0.3.)_

---

## Phase B2 — Observability + readiness

### 5. Per-tenant provider state and readiness gate _(design per 0.2)_

- [x] 5.1 Define `TenantRuntimeState` enum `{ Loading, Available, LoadingFailed { reason } }`. _(PR: `feat/b2-tenant-runtime-state` — adds `cli/src/server/tenant_runtime_state.rs` with the enum, an async-safe `TenantRuntimeRegistry`, monotonic `rev` counter that survives Loading/Failed transitions, `retry_count` on consecutive failures, and 11 unit tests.)_
- [x] 5.2 Implement the wiring model chosen in 0.2 (eager + pub/sub + lazy fallback). _(5.2a PR #118: eager boot loop `tenant_eager_wire::wire_all_known_tenants`, permissive by default, `AETERNA_EAGER_WIRE_STRICT=1` opt-in. 5.2b PR #110: Dragonfly pub/sub `tenant:changed` fan-out with instance-id self-echo filter and reconnect-with-backoff. 5.2c PR #119: `tenant_lazy_wire::ensure_wired` request-path fallback with per-slug coalescing and `AETERNA_LAZY_RETRY_COOLDOWN_SECS`-gated retry cache. 5.2-followup PR #122: `ResolverError {ConfigProviderFailed, BuildFailed}` + `try_get_*_service` fallible variants so `LoadingFailed{reason}` carries the real cause instead of a fake `Available`.)_
- [x] 5.3 Extend `/ready` (`cli/src/server/health.rs`) to include a tenant-state check in addition to current PG + vector checks. Return 503 when `failed > 0 && strict_mode`; never block on `Loading` to avoid pub/sub-induced LB flapping. _(PR #112 — `TenantCheck::from_snapshot`, sorted `failedSlugs`, reasons never leaked to the wire, `strictMode` toggled per-request so operators can flip `AETERNA_EAGER_WIRE_STRICT` without a pod restart. 7 unit tests including wire-contract camelCase guard.)_
- [x] 5.4 In tenant-scoped routes (`/api/v1/memory/*` and siblings), short-circuit to HTTP 503 `tenant_unavailable` when target tenant is `LoadingFailed`; 503 with `Retry-After: 1` when `Loading`. _(Shipped as part of the tenant-wiring stack — single chokepoint in `mod.rs::authenticated_tenant_context` calling `RequestContext::require_available_tenant` which runs `ensure_wired` then maps state→response: `Available`→pass, `Loading`→503 `loading` Retry-After:1, `LoadingFailed(reason="tenant not found")`→404, other `LoadingFailed`→503 `loadingFailed` Retry-After:30 aligned with lazy-wire cooldown. Reasons never leak: only `state` and `retryAfterSeconds` go on the wire.)_
- [x] 5.5 Admin-only wiring-state inspection endpoints. _(Implemented as the new `/admin/tenants/wiring` + `/admin/tenants/{slug}/wiring` family in `tenant_wiring_api.rs` rather than extending `/providers/status` — the latter describes **configured** providers (static), the former reflects **registry runtime state** (dynamic, per-pod). Both GETs are side-effect-free (explicitly do NOT call `ensure_wired` so "pod hasn't seen this tenant" stays distinguishable from "pod failed to wire"). PR #120. Reasons ARE exposed here — PlatformAdmin-gated — which is the authenticated surface referenced by 5.3/5.4 comments.)_
- [x] 5.6 Emit Prometheus metrics for wiring state. _(Shipped in `tenant_metrics.rs`: `tenant_state{slug,state}` gauge 0/1 per state following the `kube_pod_status_phase` idiom; `tenant_wiring_duration_seconds{result}` histogram recorded on transitions out of `Loading` (slug intentionally omitted — fleet-level concern); `tenant_state_transitions_total{from,to}` counter. Emission happens under the registry write lock so `prev→next` is atomic for the recorder. `forget()` resets all three gauge series to 0. b2-5.6-followup also landed: `metrics_util::debugging::DebuggingRecorder` + `with_local_recorder` parallel-safe capture helpers power emission assertions in unit tests.)_

### 6. First-run bootstrap hardening

- [x] 6.1 Add `BootstrapTracker` (cli/src/server/bootstrap_tracker.rs) + `GET /api/v1/admin/bootstrap/status` (PA-gated, cli/src/server/bootstrap_api.rs). `bootstrap()` instruments 6 phases — `env_and_config`, `database`, `knowledge_git`, `memory_and_providers`, `sync_and_protocols`, `redis_and_auth_stores`, `assemble_state` — and finalizes with `mark_ready()` before returning `AppState`. 14 unit tests cover wire shape, redaction, idempotency, and failure semantics. Idempotency of the underlying seed helpers (e.g. `seed_platform_admin`) is pre-existing (`ON CONFLICT DO NOTHING`) — no refactor required for the tracker to report truthfully.
- [x] 6.2 Structurally satisfied by current architecture: `bootstrap()` runs synchronously in `serve::run` **before** the HTTP listener binds, so no request can observe a mid-bootstrap state. A failing bootstrap exits the process → kubelet restart → 503 on the Service (no listener at all). `BootstrapTracker::is_completed()` is provided as a future-proofing seam for async-post-bind bootstrap refactors (see module docs in `bootstrap_tracker.rs`).
- [x] 6.3 Structurally satisfied (same reasoning as 6.2). Upstream errors from seed_* / postgres.initialize_schema propagate through `?` and crash the pod; the kubelet retry IS the restart path the task describes. Follow-up 6.4 will surface them as governance events.
- [x] 6.4 Emit `BootstrapCompleted` governance event on success (follow-up; tracker snapshot is the exact payload). _(New `GovernanceEvent::BootstrapCompleted { tenant_id, timestamp, snapshot }` variant carrying `BootstrapStatusSnapshot` (identical JSON shape to `/admin/bootstrap/status`, moved from `cli::bootstrap_tracker` into `mk_core::types` so governance-event consumers deserialize without a CLI-crate dependency). Emitted exactly once per pod from `serve::run` right after `bootstrap().await?` returns and `tracker.is_completed()`, best-effort: a DB write failure logs `warn!` and lets the server start anyway. `tenant_id` is always the `INSTANCE_SCOPE_TENANT_ID` platform sentinel. Wired into `PersistentEvent::event_type_name` as `bootstrap_completed`, `PostgresBackend::log_event`, and the Redis publisher's tenant-id match arms.)_

---

## Phase B3 — Dry-run, diff, render

### 2. Dry-run, diff, render endpoints

- [x] 2.1 Add `?dryRun=true` branch to `provision_tenant` that runs every read-only check in plan-only mode and returns a structured `ProvisionPlan`. _(Shipped in PR #126. Branch lives **after** the generation gate and **before** the first write, so dry-run preserves validate_manifest + canonical hash + short-circuit + generation-conflict as real errors but emits no writes, no `TenantCreated`/`TenantConfigChanged` events, and a dedicated `tenant_provision_dry_run` audit action. Plan contains status classifier (`create`/`update`/`unchanged`), incoming/current hash pair, current/next generation, and per-section presence flags. Structural diff per section is §2.4's remit and is blocked on full-fidelity renderer.)_
- [x] 2.2 Implement `GET /api/v1/admin/tenants/{slug}/manifest` returning the rendered current-state manifest (secret values excluded; references only). _(Shipped in `cli/src/server/manifest_render.rs` + `manifest_api.rs`. **Rendered:** `tenant` (incl. `tenant.domainMappings` as of B3 follow-up — sorted ASC via new `TenantStore::list_domain_mappings`, omitted when empty to round-trip with unmapped-tenant input), `metadata` (generation + hash), `config` (fields + secret_references), `repository`. **Not rendered (tracked in `notRendered: [...]`):** `hierarchy`, `roles`, `providers`. **Intentionally excluded from `notRendered`:** top-level `secrets:` is wire-input-only (carries plaintext `SecretBytes` the server never retains — same class as `SecretReference::Inline`); the durable form of those secrets is visible via `config.secretReferences`. PA-gated via `authenticated_platform_context`.)_
- [x] 2.3 Implement `?redact=true` mode (reference names replaced with opaque placeholders for `tenant:read`-only callers). _(Same PR as 2.2. `redact=true` replaces every `secret_references` map key and `logical_name` with a deterministic `secret-N` placeholder (indexed by sorted original-key order, so two pods produce identical output), replaces each backend-specific reference with `{"kind":"redacted"}`, and routes the repository binding through the existing `TenantRepositoryBinding::redacted()` helper. Endpoint stays PA-gated today — a `tenant:read`-scoped lower-trust tier will arrive with the scoped-tokens work in §10.)_
- [x] 2.4 Implement `POST /api/v1/admin/tenants/diff` returning structured delta between submitted manifest and current state. _(Shipped in `cli/src/server/tenant_diff.rs` + `tenant_api::diff_tenant` handler. Strategy: leverages §2.2-A–D's fully round-trippable `render_current_manifest` to reduce the diff to a pure JSON-tree walk — serialize both sides to `serde_json::Value`, walk in lockstep, emit `FieldChange{path, kind, before, after}` per differing leaf. Envelope keys (`apiVersion`, `kind`, `metadata`, `notRendered`) are stripped at the top level so schema-version drift never shows as state drift. Operation classification: `NotFound` on render → `Create`; identical JSON → `NoOp`; otherwise `Update`. 17/17 unit tests cover identical/envelope-only/create/modify/add/remove/nested/array-index/array-lengthen-shorten/type-mismatch/section-dedup/serde-round-trip/lowercase-wire. PlatformAdmin-gated like `provision_tenant`; no inline-secret gate (read-only). Consumed by B5 §7.3 `aeterna tenant diff` CLI.)_
- [x] 2.5 Add per-step `dry_run` marker to audit log (depends on 11.1). _(Shipped alongside §2.1: `AuditExtensions.dry_run: Option<bool>` lives in `storage/src/governance.rs` (line 499, column + bind + filter + read mapping all present) and every audit emission in `provision_tenant` sets it — NoOp branch → `Some(is_dry_run)`, dry-run success → `Some(true)`, three non-dry-run step emissions → `Some(false)`. Checkbox alignment only; no code change in this commit.)_

---

## Phase B4 — Secret resolution + inline gating _(depends on 0.1, 0.3)_

### 3. Secret reference resolution

- [x] 3.1 Define resolver shape per 0.1 (trait or extended alias). _(This PR — `memory/src/secret_resolver.rs`: `SecretRefResolver` async trait + `SecretResolverRegistry` dispatching by `SecretReference::kind()` + `LegacyClosureAdapter` bridging existing closures until 3.5. `ResolveError` covers `NotFound`, `BackendUnavailable`, `PermissionDenied`, `MalformedReference`, `WrongKind`. 15 unit tests — registration/overwrite, kind dispatch, unregistered-kind error surface, debug-stability, adapter kind mismatch, closure None→NotFound, inline refused, K8s/file logical-name round-trip, end-to-end registry with adapters, error-Display shape. No runtime wiring changes; closure typedef in `provider_registry.rs` stays intact until §3.5.)_
- [x] 3.2 `K8sSecretRefResolver` using the pod's in-cluster SA credentials. _(Shipped as `memory/src/secret_resolvers/k8s.rs`, 659 LOC. `K8sRefResolver<F: K8sSecretFetcher>` is generic over the transport so the resolver is unit-testable with a `MockFetcher`; `PodDownwardApiFetcher` is the production impl, reading SA token + CA cert from `/var/run/secrets/kubernetes.io/serviceaccount/{token,ca.crt,namespace}` and issuing HTTPS GET against `KUBERNETES_SERVICE_{HOST,PORT}` with `https_only(true)` + 5s timeout. Namespace precedence: explicit-on-reference → resolver default → error (never silently falls back to `"default"`). DNS-1123 path-injection guard on both namespace and name. HTTP status mapping: 200 OK / 401|403 PermissionDenied (RBAC hint) / 404 NotFound (retagged with caller TenantId by the outer resolver) / other BackendUnavailable. Base64-decodes `data.<key>` of the returned Secret. Live HTTP path feature-gated behind `k8s-secrets`; feature-off builds return a clear `BackendUnavailable` so accidental deployment surfaces cleanly. 10 tests pass.)_
- [x] 3.3 `FileRefResolver` that checks mode `<= 0600`. _(Shipped as `memory/src/secret_resolvers/file.rs`, 336 LOC. Resolves `SecretReference::File { path }` by reading an absolute path on the server's local FS. Enforces Unix mode `≤ 0600` (owner-only read/write) — group- or world-readable files return `PermissionDenied`. Additional guards: path must be absolute, target must be a regular file (directories → `MalformedReference`), size cap to prevent OOM on runaway paths (oversized → `MalformedReference`), missing file → `NotFound` retagged with tenant. 10 tests including `world_readable_file_is_permission_denied`, `group_readable_file_is_permission_denied`, `reads_owner_only_file`, `missing_file_is_not_found`, `directory_is_malformed`, `oversized_file_is_malformed`.)_
- [x] 3.4 `EnvRefResolver` and `VaultRefResolver` (Vault feature-gated stub). _(Both shipped. `EnvRefResolver` in `memory/src/secret_resolvers/env.rs` (170 LOC, 6 tests) reads `SecretReference::Env { var }` from `std::env::var`, rejects empty var names, retags `NotFound` with tenant. `VaultRefResolver` real impl in `memory/src/secret_resolvers/vault.rs` behind `#[cfg(feature = "vault")]` (vaultrs-based); the `vault_stub.rs` (107 LOC, 3 tests) is re-exported on default builds so the module's public surface is stable across feature flags — every `resolve` returns `BackendUnavailable { reason: "feature disabled" }`. Both resolvers integrate cleanly with `SecretResolverRegistry` alongside the K8s and File backends. 37/37 tests pass across the full resolver suite.)_
- [x] 3.5 Wire chosen model into the per-request secrets provider. _(Two-phase landing on `feat/b4-secret-resolver-trait-and-cli-validate-decisions`. Phase A (ab6b511e): `set_secret_resolver_registry` added, registry + 6 resolver impls wired in `bootstrap.rs` alongside the legacy closure. Phase B (6c03c71b): `pub type SecretResolver` + `ClosureConfigAdapter` + `TenantProviderRegistry::set_resolvers(c, s)` DELETED; replaced by `RegistryConfigAdapter` which does logical-name → `TenantSecretReference` lookup against the tenant config document and dispatches through the typed registry by `SecretReference::kind()`. New resolvers: `InlineRefResolver` (dev/test), `PostgresRefResolver` (wraps `Arc<dyn SecretBackend>`). Behavioural change: adapter now surfaces non-NotFound resolver errors as `Err` instead of silently coercing to `None` — previously hidden permission/backend failures now propagate to operator logs.)_
- [x] 3.6 Add systematic coverage that resolved plaintext is never logged, never serialized into responses, and never cached beyond request scope. Add redaction tests. _(New `secret_resolver::redaction_coverage` module with 10 tests: (1–4) each `ResolveError` variant pinned for plaintext-cannot-leak-by-construction (`&'static str`-only fields) vs plaintext-could-leak-via-author-error (`String` reason fields), (5) end-to-end `SecretBytes` redaction through the registry's `Debug`/`Display`/`Serialize` paths — the three channels HTTP response bodies and logs take, (6) `SecretReference::Inline { plaintext }` redacts through JSON serialize + `Debug`, (7) `LeakyResolver` canary documenting the type-system boundary (resolver authors must not embed plaintext in `reason` strings — the types cannot enforce it, only code review can), (8) `EnvRefResolver` missing-var error path leaks nothing, (9) zeroize-on-drop pin (delegated to `mk_core::secret::tests` — re-declaring `zeroize` in `memory/Cargo.toml` would split-version), (10) permission-denied + malformed-reference error rendering. Uses a `SENTINEL_PLAINTEXT = "hunter2-DO-NOT-LEAK-swordfish"` + `assert_no_plaintext` helper that checks both raw and JSON-escaped forms — the two shapes a grepping operator would recognise in a log line.)_

### 4. Inline-secret gating

- [x] 4.1 Add `allow_inline_secret: bool` to server config, default `false`, off in release builds. _(New `ProvisioningConfig` section on `Config` with `allow_inline_secret: bool` (default `false`). Release-build hard-off enforced in `ProvisioningConfig::effective_allow_inline_secret()` via `cfg!(debug_assertions)` — release pods ignore the flag regardless of YAML/env and cannot be opened by query parameter, header, or any other runtime path. Unit tests pin both the default and the build-profile behaviour.)_
- [x] 4.2 Accept `?allowInline=true` on provision only when the server flag is set. _(New `allowInline: Option<bool>` on `ProvisionQuery`. Handler computes `server_allows && caller_allows` — either side missing and the gate closes. Integration test `provision_rejects_inline_secret_when_only_query_flag_is_set` locks down "query flag alone is not enough".)_
- [x] 4.3 Reject non-empty `secrets[].secretValue` unless both conditions hold; return actionable error pointing to `config.secretReferences`. _(HTTP 422 with stable error code `inline_secret_not_allowed`. Body enumerates offending `logicalName`s (never values or lengths — would be a side-channel), and a `remediation` block names both the preferred path (`config.secretReferences`) and the exact two gates an operator must flip (`provisioning.allowInlineSecret` + `?allowInline=true`) with their current state. Empty-byte `secretValue` entries still pass — that shape is the migration target.)_

---

## Phase B5 — CLI refactor _(depends on 0.4)_

### 7. CLI: apply, render, diff, validate, watch

- [x] 7.1 Add `TenantCommand::Apply(TenantApplyArgs)` with `-f/--file`, `--dry-run`, `--prune`, `--watch`, `--wait`. _(Core `tenant apply` surface shipped. Wraps `POST /admin/tenants/provision` (write path). Flags: `-f/--file` (supports `-` stdin), `--yes` (skip interactive confirmation; required for non-TTY + `--json`), `--json` (raw server JSON for CI, forces `--yes`), `--allow-inline` (passes `?allowInline=true` for dev-mode inline plaintext), `--target-tenant` (PA cross-tenant context, NOT a slug override — `manifest.tenant.slug` is always authoritative). Two-stage flow: interactive mode runs a dry-run preview first, shows the `ProvisionPlan`, prompts `y/N`, short-circuits "unchanged" without firing the real apply; `--json` mode skips the preview. Race model documented in code: preview is advisory; the guarded UPDATE at the tail of `provision_tenant` is authoritative — a concurrent apply between preview and confirm surfaces as a rendered HTTP 409 `generation_conflict`, not a crash. New `AeternaClient::tenant_apply` tolerates 200/207/409/422 as structured-body responses (only bails on 5xx/auth/transport); mirrors the 200/422 dual-success pattern of `tenant_provision_dry_run`. Classifier (`ApplyOutcome` enum) maps `(success, status, error)` to one of 7 outcomes (Applied/Unchanged/Partial/GenerationConflict/ValidationFailed/InlineSecretRejected/Other) — `manifest_parse_failed` and `manifest_validation_failed` are deliberately grouped under `ValidationFailed` so the CLI renders both with the same affordance. `render_apply_result` emits steps table with ✓/✗ icons; `render_generation_conflict` shows current vs submitted + hint; `render_inline_secret_rejected` lists offending logical names. 15 new unit tests (classifier, renderers, clap surface) — 56/56 `commands::tenant` tests pass. `tenant validate --file` (dry-run preview) was the first CLI consumer of the dry-run endpoint and remains the interactive preview path; it is now also invoked internally by `tenant apply` before prompting. **Update (follow-on PRs):** `--watch` shipped in #160 (co-scheduled SSE stream during apply, 250 ms pre-apply buffer to avoid races on fast-path provisions, `--json` mode emits frames on stderr to keep stdout = single apply response). `--watch-timeout=<secs>` shipped in #161 (stall detection: resets on every chunk, not every parsed frame — batched chunks are one reset which is the correct live-server signal; emits structured `watch_stall` error with last-event metadata). `--watch-until=<EVENT>` shipped in #162 (post-apply wait for async reconciliation; accepts bare kinds and `step:<name>` form where the `step:` variant requires `status=="ok"` to avoid tripping on opening `started` frames). The `--watch` trio closes the operational loop for async-reconciliation flows (IAM sync, DNS propagation). **Still not in scope:** `--prune` (drift removal — follow-up drift-gate task) and `--wait` (now subsumed semantically by `--watch-until`; no separate flag needed).)_
- [x] 7.2 Add `TenantCommand::Render(TenantRenderArgs)` with `--slug`, `--redact`, `-o`. _(Shipped: `aeterna tenant render [--slug <slug>] [--redact] [-o <file>] [--target-tenant <slug>]` calls `GET /api/v1/admin/tenants/{slug}/manifest[?redact=true]` via a new `Client::tenant_manifest` helper and emits pretty-printed JSON with a trailing newline — identical bytes whether piped via shell redirection or `-o`. Slug resolution mirrors `tenant show`: explicit `--slug` wins, else falls back to `profile::load_resolved(None, None).tenant_id` (env → project `.aeterna/context.toml` → user profile → server default). `--redact` forwards verbatim as `RenderQuery.redact=true`; plaintext secrets are never emitted regardless (the server has no access to unwrapped values — `redact` only hides operator-chosen logical names). No `--output json|yaml` flag yet: the server emits JSON natively and `-o` is path-valued rather than format-valued; a later follow-up can add `--output yaml` once there's a consumer. Pre-existing cwd-race in `test_read_local_context_tenant_{absent,present}` (process-global `set_current_dir` without a serial gate) was fixed in the same commit with a `OnceLock<Mutex<()>>` guard — no new dev-dep pulled in.)_
- [x] 7.3 Add `TenantCommand::Diff(TenantDiffArgs)` with `--slug`, `-f`, `-o unified|json`. _(Shipped in PR #156. `aeterna tenant diff -f <manifest> [-o unified|json] [--target-tenant <slug>]` calls `POST /api/v1/admin/tenants/diff` via a new `Client::tenant_diff` helper and renders the server's `TenantDiff` response (see `cli/src/server/tenant_diff.rs`) in one of two shapes. `--slug` is implicit — the diff target is pulled from `manifest.tenant.slug` server-side, same as `tenant validate` / `tenant apply`; `--target-tenant` selects the admin context, NOT the slug being diffed. Unified renderer (`render_diff_unified`) formats each `FieldChange` as one line with `+`/`-`/`~` prefixes plus dot-notation paths; JSON mode pretty-prints the raw `TenantDiff` struct (stable wire format backed by 22 server-side tests). 200/422 dual-success handling mirrors `tenant_provision_dry_run`: validation failures on 422 surface as a non-zero exit with the standard `render_validation_errors` projection, and `--output json` passes the 422 body through verbatim so CI can discriminate "manifest invalid" from "diff computed, changes detected". 5 unit tests cover noop/create/update shapes, complex value formatting, and forward-compat for unknown `ChangeKind` values emitted by a future server.)_
- [x] 7.4 Add `TenantCommand::Validate` per the decision in 0.4. Reconcile with existing nested `tenant repo-binding validate` (L82) and `tenant config validate` (L94). _(Top-level `TenantCommand::Validate` was shipped alongside §7.1 in the early B2 waves — it wraps `tenant_provision_dry_run` and exits non-zero on 422 for CI gates. This PR closes the reconciliation half of the task per §0.4 (design.md §D8): the two nested validate handlers (`tenant repo-binding validate`, `tenant config validate`) now emit a one-line stderr deprecation warning pointing operators at `tenant validate --file <manifest>`, via a new `print_nested_validate_deprecation` helper. The warning is suppressed in `--json` mode (stdout must stay byte-clean for pipeline consumers). Deliberately minimal: this PR does NOT route the narrow payloads through the top-level dry-run path — that wrapping ("minimal-manifest apply") is §7.6's scope and changes hands with the same-PR migration of the other 6 legacy subcommands. The warning ships ahead of the code change so operators can migrate scripts before the behaviour shift. Scheduled removal: v0.10. 1 unit test pins both `json_mode=true` (must be silent) and `json_mode=false` (must not panic) branches.)_
- [x] 7.5 Add `TenantCommand::Watch(TenantWatchArgs)` streaming per-step status. _(Shipped in PR #159. `aeterna tenant watch --slug <slug> [--json]` subscribes to `GET /api/v1/admin/tenants/{slug}/events` (SSE, `text/event-stream`) and streams lifecycle frames (`provisioned`, `updated`, `deactivated`, `lagged`, `provisioning_step`) as they arrive. Inline SSE parser handles multi-line `data:` joins, event name defaulting, and chunk-boundary fragmentation — `reqwest::bytes_stream` does not guarantee frame alignment. Ctrl-C closes the subscription cleanly. Follow-on PRs #160/#161/#162 reused the same parser + client helpers to implement `tenant apply --watch`, `--watch-timeout=<secs>`, and `--watch-until=<event>` (see §7.1 bullet-list body, now accurate).)_
- [x] 7.6 Re-implement `Create`, `Update`, `DomainMap`, `RepoBinding`, `Config`, `Secret`, `Connection` internally as minimal-manifest `apply` invocations, preserving their CLI surface. _(**Scope deviation: the CLI surface was NOT preserved** — per explicit operator decision ("break the whole cli subcommands, I don't care about breaking changes"), the mutation subcommands were **removed outright** instead of being rewrapped as thin apply-wrappers. Rationale: thin-wrapper variant keeps ~1100 LOC of per-field flag handling, manifest-assembly, and dual test surfaces; outright removal collapses all mutation flow through a single well-tested path (`/provision` + `TenantApplyArgs`) and eliminates ongoing drift risk between the two shapes. 11 mutation handlers deleted (`run_create`, `run_update`, `run_domain_map`, `run_repo_binding_set`, `run_repo_binding_validate`, `run_config_upsert`, `run_config_validate`, `run_secret_set`, `run_secret_delete`, `run_connection_grant`, `run_connection_revoke`), along with 10 Args structs (`TenantCreateArgs`, `TenantUpdateArgs`, `TenantDomainMapArgs`, `TenantRepoBindingSetArgs`, `TenantRepoBindingValidateArgs`, `TenantConfigUpsertArgs`, `TenantConfigValidateArgs`, `TenantSecretSetArgs`, `TenantSecretDeleteArgs`, `TenantConnectionGrantArgs`, `TenantConnectionRevokeArgs`), the `TenantSecretCommand` enum entirely, and the §7.4 deprecation-warning helper (`print_nested_validate_deprecation`) which became dead code after its two call sites were deleted. Also pruned: 10 unit tests that exercised the now-dead argstructs. **Kept**: all read surfaces (`list`, `show`, `repo-binding show`, `config inspect`, `connection list`), all manifest-pipeline commands (`validate`, `render`, `diff`, `apply`, `watch`), user-context (`use`, `switch`, `current`), and the lifecycle op `deactivate` (not cleanly expressible as a manifest field in v1; retained as a standalone command). **§7.6 follow-up correction:** the initial sweep also deleted `tenant connection grant` and `tenant connection revoke`, but Git-provider-connection visibility is NOT expressible through the v1 `TenantManifest` (the `git_provider_connections_tenants` junction is not touched by `/provision`); both subcommands were restored in the §7.x follow-up PR with module-level docs explaining why they are intentionally standalone. Total: -1264 lines across `cli/src/commands/tenant.rs` (5259 → 3995). New `docs/guides/tenant-cli-migration-v0.9.md` documents the removed surface, the manifest-field equivalence for each removed subcommand, a before/after migration example, and a rollback hint (pin to v0.8 CLI — server remains backward-compatible for at least one minor version). Docs under `website/docs/guides/*` and `docs/guides/*` and `INSTALL.md` still reference the old commands — intentionally left for a follow-up docs-only PR since the scope there is surface-rewrite, not behavioural. 974/974 `cargo test -p aeterna --lib` pass; `cargo fmt --check` ✅; `cargo check --all-targets` ✅. The underlying per-resource server endpoints (`PUT /admin/tenants/{slug}/repo-binding`, `PUT /admin/tenant-config`, etc.) are **not** removed — they still back the admin-UI's per-section save flows and may be deprecated separately under §2.10 once `/provision` covers all UI write paths.)_
- [x] 7.7 Inject `X-Aeterna-Client-Kind: cli` and `User-Agent: aeterna-cli/<version>` on every HTTP request. _(Canonical `AeternaClient` installs both headers via `default_headers()` + `user_agent()` on a shared builder (`build_http_client`) — static consts, `from_static` cannot fail, `build()` wrapped in `expect` to panic loudly rather than fall back to untagged traffic. This PR closed three CLI-originated leak sites that were bypassing the builder with bare `reqwest::Client::new()`: the health reachability probe in `offline.rs::check_server_reachability` and the unauthenticated admin export + import byte-stream endpoints in `commands/admin.rs`. Promoted `build_http_client` to `pub(crate) tagged_http_client` so CLI call sites that do not use the full authenticated `AeternaClient` surface (no profile, no retry, no token refresh) can still emit §7.7 attribution. New unit test `client_kind_tagging_tests::tagged_client_constants_are_wired` pins the two header values so a future rename breaks the test rather than silently de-attributing the audit log. Drive-by: fixed pre-existing master breakage in `server_runtime_test.rs` (`AppState` initializer missing `revocation_cache` field — added `Default::default()` matching the other 8 test-scope initializers).)_

### 8. CLI: secure secret input

- [x] 8.1 Add `--ref`, `--from-file`, `--from-stdin`, `--from-env` flags on `tenant secret set`. _(New `cli::secret_input` module with a `SecretInputFlags` + `resolve()` pair driving a `SecretPayload` enum (`Inline` → `secretValue`, `Reference` → `secretRef`). All four new flags plus the gated `--value` wired into `TenantSecretSetArgs` with doc comments that state the threat model each mode addresses. Exactly one source must be supplied: zero → UX error with copy-pasteable fixes, two-or-more → UX error with the exact count so the user sees the ambiguity.)_
- [x] 8.2 Reject `--value` unless `--allow-inline-secret` is also set. _(Inline bytes leak into shell history, `ps`, and CI logs; the resolver rejects `--value` without `--allow-inline-secret` with a UX error that names all four safer modes and suggests `--from-stdin` by default.)_
- [x] 8.3 `--from-file` checks mode `<= 0600`. _(Check is `mode & 0o177 == 0` — captures "no world bits, no group bits, no owner-execute bit" in one expression. Tests pin `0600` and `0400` to accepted, and `0644`/`0660`/`0700` to rejected with a copy-pasteable `chmod 0600 <path>` suggestion in the error. UTF-8 validation + trailing-newline stripping (LF and CRLF, exactly once, preserving other trailing whitespace) + empty-file rejection all covered. On non-Unix platforms the mode check returns `None` and skips — metadata still stats for "file not found".)_
- [x] 8.4 `--from-stdin` disables terminal echo when stdin is a TTY. _(`RealSecretIo::read_stdin_secret` branches on `std::io::IsTerminal`: TTY → `dialoguer::Password` (stars + echo off), pipe → `read_to_string` + strip single `\n`/`\r\n` terminator. Resolver logic is driven through a `SecretIo` trait seam so unit tests script stdin deterministically without touching the real terminal.)_
- [x] 8.5 `--from-env` reads the named variable and clears it from child processes. _(`RealSecretIo::clear_env` calls `unsafe { std::env::remove_var }` after a successful read — Rust 2024 marks env mutation `unsafe` because it is not thread-safe; a SAFETY comment justifies it by pointing at the single-threaded CLI dispatch path. Tests assert the var is both returned to the caller AND removed from the env map so child processes cannot inherit. Empty values and unset variables fail loudly; an all-whitespace `--from-env` name is rejected before any lookup.)_

_§8 implemented as `cli/src/secret_input.rs` (new, 600+ lines inc. tests) wired into `run_secret_set` through a `SecretIo` trait seam — 24 unit tests cover every branch including five-source ambiguity, CRLF stripping, the negative-space 0700 case, and env-clear after read._

### 9. CLI: output and exit codes

- [x] 9.1 Extract `output::Renderer` supporting `table`, `json`, `yaml`, `name`, `jsonpath=<expr>`. _(New `cli::output::renderer` submodule; split the flat `output.rs` into `output/mod.rs` (keeps existing `header`/`info`/`warn` printers) + `output/renderer.rs` and re-export the new types. Three public types: `OutputFormat` (`Table`/`Json`/`Yaml`/`Name`/`JsonPath(String)`), `Renderer`, and error enums (`ParseFormatError`, `JsonPathError`, `RenderError`). `OutputFormat::parse(raw)` is case-insensitive for bare keywords; `jsonpath=<EXPR>` takes everything after the first `=` verbatim (so filter syntax like `=@.x==true` survives) and rejects empty expressions at parse time. The kubectl-style JSONPath extractor supports optional `{}` wrapper, `.foo.bar` object navigation, and `[N]` numeric indexing only; wildcards (`[*]`), filters (`[?(...)]`), slices (`[a:b]`), and recursive descent (`..`) return `UnsupportedConstruct` with a clear message pointing at `jq` as the escape hatch. Strings extracted via JSONPath render without quotes so `-o jsonpath={.name}` is shell-pipelineable without post-processing.)_
- [x] 9.2 Default to `table` on TTY, `json` otherwise, unless `-o` specified. _(`OutputFormat::default_for_tty(is_tty: bool)` returns `Table` on TTY and `Json` on pipe — matches `kubectl`/`gh`/`aws`/`terraform`. Callers wire `std::io::stdout().is_terminal()` through; both directions pinned by unit tests so future refactors cannot silently flip the default.)_

_§9.1/§9.2 landed as primitives only: 30 unit tests, `cargo clippy --all-targets -- -D warnings` clean. Migrating the existing `if args.json { ... }` call sites to use the new renderer is a deliberate follow-up so this PR can be reviewed in isolation._
- [x] 9.3 Replace ad-hoc exit codes with a standard table (0/1/2/3/4/5); add `ExitCode` enum + unit tests. _(New `cli::exit_code::ExitCode` with `#[repr(u8)]` discriminants frozen to the documented table: `Success=0`, `Usage=1`, `AuthDenied=2`, `Conflict=3`, `ServerTransient=4`, `ServerFatal=5`. `ExitCode::exit()` centralises the `std::process::exit` call so `grep 'process::exit' cli/` stays a reliable audit surface. All 12 pre-existing `std::process::exit(1)` call sites in `commands/{check,govern,knowledge,admin}.rs` migrated to `ExitCode::Usage.exit()` — semantics preserved, the numbers now live in one place.)_
- [x] 9.4 Map HTTP status codes to CLI exit codes consistently (401/403 → 2, 409 → 3, 5xx → 4/5, 4xx schema → 1). _(`ExitCode::from_http_status(u16)` takes a raw `u16` so callers do not need to drag `reqwest`/`http` into the exit-code module. Splits 5xx by retry semantics: `500/501` and unknown 5xx → `ServerFatal` (do not hot-loop), `502/503/504` → `ServerTransient` (safe to back off and retry). Explicit negative assertions pin `402`/`407`/`410`/`412` to `Usage` — close relatives of auth/conflict that scripts specifically must NOT confuse. 11 unit tests cover discriminant stability, every documented row of the table, and out-of-range defensive handling.)_

---

## Phase B6 — Scoped tokens

### 10. Scoped tokens

- [x] 10.1 Add `token_type` and `scopes` **JWT claims**. Both fields live on `PluginTokenClaims` with `#[serde(default)]` for rolling-deploy compatibility; `PluginIdentity` propagates them; the OAuth user-flow minter sets `token_type: "user"` + `scopes: []`. Service-token minter (§10.2) is the other writer. Constants `TOKEN_TYPE_USER|SERVICE|REFRESH` and `AUDIENCE|KIND` anchor the contract.
- [x] 10.2 Implement `POST /api/v1/auth/tokens` (PlatformAdmin only) with bounded `expiresIn` (max 24h, default 4h). _(PR #149 — `cli/src/server/service_tokens.rs::mint_handler`. PA-gated; TTL bounded to `[60s, 86400s]` with 4h default; capabilities validated against 15-entry allow-list mirrored from `agent-delegation.cedar`. Service tokens persisted as `agents` rows with `agent_type='service'`; JWT reuses `PluginTokenClaims` with `token_type="service"`. 15 unit tests: name bounds, capability allow-list, TTL bounds, JWT round-trip with right/wrong secret, allow-list uniqueness.)_
- [x] 10.3 `DELETE /api/v1/auth/tokens/{tokenId}` with a 60s revocation cache TTL across instances. _(PR #149 shipped the DELETE handler flipping `agents.status='revoked'`; this PR adds the cross-instance revocation cache in `cli/src/server/service_token_validator.rs`, backed by Redis/Dragonfly (`SET revocation:agent:<uuid> EX 60` / `DEL` on revoke) with an in-process `DashMap` fallback for single-instance/dev. Revocation is zero-staleness across instances in HA mode; TTL bounds Postgres drift to 60s.)_
- [x] 10.4 `GET /api/v1/auth/tokens` listing non-expired tokens (without raw strings). _(PR #149 — `cli/src/server/service_tokens.rs::list_handler`. PA-gated; returns metadata only (token_id, name, tenant_id, capabilities, status, expires_at, created_at, created_by). Expired rows filtered server-side; revoked rows hidden by default, surfaced via `?includeRevoked=true` for audit UIs. Optional `?tenantId=<slug-or-uuid>` filter.)_
- [~] 10.5 Middleware that checks `scopes` claim against the route's required scope; fall back to role check for user callers. _(Keystone routes done, follow-up wiring pending. **Landed:** Validator + capability-gate API in `service_token_validator::{validate_service_token_from_headers, require_capability}`; mint/revoke handlers warm/evict the Redis revocation cache. Per-route helper `require_platform_admin_or_scope` in `tenant_api.rs` — returns an `AuthPrincipal` enum so handlers can thread the authenticated principal through the full request lifetime. Wired on `POST /admin/tenants/diff` (scope `tenants:diff`) and `POST /admin/tenants/provision` (scope `tenants:provision`) — the provision mutation was unblocked by the §11.4 audit-actor refactor below. **Not yet wired (mechanical follow-up):** `list_tenants`/`get_tenant` (scope `tenants:read`), `get_tenant_manifest` (render), `validate_tenant_manifest` (scope `tenants:validate`), `watch_tenant_events` (scope `tenants:watch`), `grant_connection`/`revoke_connection` (scope `connections:manage`). Read-only routes (list/get/manifest/validate/watch) don't write audit rows so wiring them is just swapping the auth helper; connection mutations will pick up the `AuditActor` plumbing for free. Integration coverage for scope-accepted callers is folded into the §13 consistency suite.)_
- [x] 10.6 Reject `--token` flag in the CLI; read tokens from `AETERNA_API_TOKEN`, OS keychain, then `~/.aeterna/credentials` (mode-gated). _(Pre-clap scan in `cli/src/main.rs::reject_legacy_token_flag` catches `--token`, `--token=…`, `-t`, `-t=…` before clap parses, so operators get a UX-error with migration guidance naming both supported paths (env var + `aeterna auth login`) instead of clap's generic "unexpected argument" message. The scanner returns a fixed-shape `&'static str` identifier — never the caller's actual token value — so a user pasting the rejection output into a support channel cannot also leak their credential (dedicated regression test). Substring-match guard: `--token-file`, `--service-token`, `--use-token=yes`, `--tokenize`, `-tx`, `-token` are all accepted (clap handles them). Exit code: `ExitCode::Usage` (1). Token resolution in `AeternaClient::from_profile` now consults `AETERNA_API_TOKEN` first (new constant in `env_vars.rs`) — opaque bearer used verbatim, no refresh attempted (no refresh_token available); empty env value falls through to the credentials file so a stale `.env` does not break interactive login. Credentials file (`~/.config/aeterna/credentials.toml`) remains the interactive-login path with refresh-token lifecycle. 8 new unit tests in `main.rs::tests` (clean-argv positive cases, each of 4 flag shapes rejected, value-not-leaked invariant, first-match-wins determinism, substring-match negative cases). 946/946 `aeterna` binary tests pass. **Not in this PR:** OS keychain integration via `keyring` crate — already called out as future work in `credentials.rs` doc-comment; it is mode-gated per the task spec and blocked on a workspace dep addition. Env + file are shipped; keychain is the third-tier fallback that operators who care can build on top without another code change to the resolution order.)_

---

## Phase B7 — Audit + UI + tests + docs (parallel PRs)

### 11. Audit parity

- [x] 11.1 Extend audit log schema with `via`, `client_version`, `manifest_hash`, `generation`, `dry_run`.
- [x] 11.2 Extract `X-Aeterna-Client-Kind` header in router and propagate through request-scoped audit context.
- [x] 11.3 Normalize unknown client-kind values to `api`, preserving original in `client_kind_raw`.
- [x] 11.4 Ensure every provision-path mutation records the new fields. _(Extended in-place to teach the audit helpers about non-user principals. `tenant_api.rs` now defines an `AuditActor<'a>` enum with `User(&TenantContext)` and `Service(&ServicePrincipal)` variants plus `From` impls so every pre-existing `audit_tenant_action(&state, &ctx, ...)` call site keeps compiling via `impl Into<AuditActor<'_>>` — zero churn on the ~25 callers outside the provisioning handlers. The central `audit_actor_fields` function resolves an actor to the `(PrincipalType, actor_id, acting_as_tenant_id, envelope)` tuple consumed by `log_audit_with_extensions`: User callers record as `PrincipalType::User` with `actor_id = user_uuid` (unchanged), Service callers as `PrincipalType::Agent` with `actor_id = agent_uuid` and `envelope.actorKind = "service"` / `envelope.agentId = <uuid>`. The envelope additionally carries `actorKind` for both paths (strict-additive — existing consumers that read `actorTenantId` / `selectedTargetTenantId` / `details` are unaffected). `AuthPrincipal::to_downstream_tenant_context(target)` synthesizes a `mk_core::types::TenantContext` for downstream org-hierarchy ops when the caller is a service principal: `user_id = agent_uuid`, empty roles (the scope check at the gate is authoritative — downstream MUST NOT re-check role), `agent_id = Some(agent_uuid)`. Five new unit tests pin the contract: user-path shape matches pre-§11.4, target-tenant preference for `acting_as`, service-path agent attribution, `From` coercion coverage for both directions, and downstream-ctx synthesis for service principals. 979/979 `aeterna` lib tests pass.)_

### 12. Admin UI wizard

- [x] 12.1 Add a "Create tenant" wizard (multi-step form) that composes a `TenantManifest` client-side. _(Shipped as `admin-ui/src/pages/tenants/wizard/CreateTenantWizard.tsx` + `steps.tsx` + `admin-ui/src/api/tenant-manifest.ts`. The wizard orchestrates 5 steps + preview + submit + result panels; state is a single `TenantManifest` object with `pruneManifest()` removing empty sub-sections before display/submit. Replaces the prior minimal `CreateTenantDialog` (which only POSTed `{slug, name}` to the legacy `/admin/tenants` route).)_
- [x] 12.2 Step 1: tenant identity (slug, name, domain mappings). _(`IdentityStep` — slug pattern-validated to `[a-z0-9-]+`, name required, domain mappings as add/remove list. Validator rejects empty slug/name and bad slug chars.)_
- [x] 12.3 Step 2: secret reference picker. _(`SecretsStep` — picker for the five reference kinds (`env` / `file` / `k8s` / `postgres` / `inline`) with kind-specific structured fields and a per-kind validator. **Note on the backend endpoint**: the spec called for a new `GET /api/v1/admin/secret-sources` returning available source names. Skipped in this commit because the server today has no central registry of "available sources" to enumerate (the K8s/file/env code paths each resolve at request time). Tracked as a §12.3-followup: introduce a `SecretSourceRegistry` in the bootstrap path that surfaces per-kind availability + sample names. The wizard already produces fully-formed references via the structured picker; the registry endpoint will later light up auto-complete.)_
- [x] 12.4 Step 3: initial hierarchy. _(`HierarchyStep` — flat first-pass for top-level units (Company/Organization/Team/Project). Tree editing (nested children, member binding) deferred to the org-tree page already shipped in §6, since that page handles the full CRUD for an existing tenant. The wizard's job is bootstrap, not full hierarchy authoring.)_
- [x] 12.5 Step 4: role assignments. _(`RolesStep` — email + role from the canonical role set, tenant-wide scope. Per-unit scope intentionally deferred: a wizard is the wrong UX for unit-path picking before the hierarchy is committed.)_
- [x] 12.6 Step 5: provider declarations. _(`ProvidersStep` — opt-in toggles for `llm` and `embedding` providers with kind + model fields. Memory layers configuration is left to the existing tenant-detail config tab (where it lives today).)_
- [x] 12.7 Preview screen: render composed manifest as YAML. _(Reuses `manifestToYaml()` from `tenant-manifest.ts` — a minimal JSON→YAML pretty-printer good enough for on-screen preview. Wire submit is JSON; YAML is purely a UX affordance.)_
- [x] 12.8 Submit screen: POST with `X-Aeterna-Client-Kind: ui` and per-step progress. _(Submit goes via raw `fetch()` rather than `apiClient` so the `X-Aeterna-Client-Kind: ui` header lands cleanly. The provision response's `steps[]` array is rendered as a checklist with ok/skipped/error glyphs. Inline-secret detection drives an opt-in `?allowInline=true` toggle that mirrors the server-side gate (§4.2).)_
- [x] 12.9 "Download manifest" action on tenant detail page. _(New `Manifest` tab on `TenantDetailPage` with two render buttons (`?redact=true` default, plain explicit) hitting the existing `GET /admin/tenants/{slug}/manifest` endpoint, plus a `Download YAML` button that produces `<slug>.manifest[.redacted].yaml` via a Blob+anchor click. Three Playwright e2e tests pin the wizard happy path, the inline-secret query-string flag, and the manifest download flow; all 8 admin-ui e2e tests pass.)_

### 13. Consistency acceptance suite

- [x] 13.1 Create `tests/tenant_provisioning/scenarios/` with at least five fixtures (bootstrap, add-company, rotate-reference, no-op re-apply, prune). Five JSON fixtures landed under `tests/tenant_provisioning/scenarios/` forming a sequenced narrative against slug `acme-bootstrap`: create → extend → modify → no-op re-apply (byte-identical) → prune. JSON (not YAML) is the canonical form because the wire protocol is JSON; each fixture carries `metadata.labels.suite: consistency` so the runners can filter stray manifests.
- [x] 13.2 Implement `runner_api.rs` (direct POST with test-minted scoped token). Landed as `cli/tests/tenant_provisioning_consistency_test.rs` driving the in-process axum router (built via `router::build_router(state)` against a testcontainer Postgres) through every fixture in sequence: 01 → 02 → 03 → 04 → 05. Each step submits the manifest to `POST /api/v1/admin/tenants/provision`, then re-renders via `GET /api/v1/admin/tenants/{slug}/manifest?redact=true` and structurally diffs the round-trip. Auth uses the existing `x-user-id`/`x-user-role: platform_admin` test headers; scoped-token minting (§10.2) is deferred until that subsystem reaches production use, at which point this runner swaps the headers for a `Bearer` token without touching the assertions. The runner also pins the `04-noop-reapply` invariant (`status: "no_op"` MUST be returned) and the `?allowInline=true` rejection gate from §4.2. Test harness factored into a new `cli/tests/common/mod.rs` so the future CLI/UI runners can share the same `build_test_state()` builder.
- [x] 13.3 Implement `runner_cli.rs` (spawns `aeterna tenant apply`). Landed as `cli/tests/tenant_provisioning_consistency_cli_test.rs`. Drives every fixture through the real `aeterna` binary (`tenant apply -f <fixture> --yes --json` followed by `tenant render --slug <slug> --redact`) against a background-bound `axum::serve` server on a random `127.0.0.1:0` port. The test seeds a `users` row with `idp_provider='github'` + `idp_subject='platform-admin-runner'` and a `platformadmin` role at `INSTANCE_SCOPE_TENANT_ID`, mints an HS256 JWT with the matching `sub` claim, and exports it as `AETERNA_API_TOKEN` so the spawned CLI process authenticates via the documented service-token env-var path. The `redact_volatile()` allowlist + `assert_round_trip()` helpers are duplicated locally rather than extracted into a shared module so the CLI and API runners can evolve independently. The fourth fixture (`04-noop-reapply`) is asserted to return `status == "no_op"` — same sequence invariant as §13.2.
- [ ] 13.4 Implement `runner_ui.rs` (Playwright against `/admin/*`). Deferred — the wizard from §12 is in place (`admin-ui/e2e/tenant-wizard.spec.ts`) and already drives the same `POST /provision` contract through the browser, so the missing piece is purely the matrix-runner wrapper that loads each fixture and asserts the rendered manifest. Same fixture set, same allowlist.
- [x] 13.5 Implement `assertions.rs` that renders the tenant and diffs against an expected baseline; allowlist timestamps and IDs. Landed as `redact_volatile()` + `assert_round_trip()` inside `tenant_provisioning_consistency_test.rs`. Allowlist: `id`, `createdAt`, `updatedAt`, `deactivatedAt`, `generation`, `sourceOwner`, `status`, `success`, `steps`, `message`, `tenantId`, `unitId`, `userId` — applied recursively through arrays and nested objects. Assertions cover slug, name, domain mappings, every input `config.fields[k]` and every `config.secretReferences[k]`. Unit-tested by `redact_volatile_strips_allowlist_recursively` so the allowlist itself does not silently rot.
- [x] 13.6 CI job `consistency-matrix` running all scenarios through all three runners in parallel. Landed as `.github/workflows/tenant-provisioning-matrix.yml` with a `strategy.matrix.runner: [api]` axis (fail-fast: false). Triggers on `pull_request` and `push` to master for any change under `cli/`, `crates/`, or `tests/tenant_provisioning/`. The workflow is pre-shaped for fan-out: adding `cli` and `ui` to the matrix is a one-line change once §13.3/§13.4 land. Job verifies Docker availability up-front (testcontainers prerequisite) and runs the suite in `--release` with `--test-threads=1` so the sequenced fixtures don't race against the same slug.
- [x] 13.7 CI job running a fast pre-check against every fixture. Implemented as `tests/tenant_provisioning/run_validate.sh` (client-local jq-based structural validator — not the server-backed `aeterna tenant validate`, which requires a live control-plane and is covered by §13.2–§13.4 instead) and wired into `.github/workflows/tenant-provisioning-fixtures.yml`. The script enforces parseable JSON, `apiVersion: aeterna.io/v1` + `kind: TenantManifest`, non-empty `tenant.slug`/`tenant.name`, and the `metadata.labels.suite == "consistency"` opt-in marker. No Docker, no Postgres, no built binary required.

### 14. Documentation

- [x] 14.1 `docs/tenant-provisioning.md` — lifecycle (bootstrap → render → edit → diff → apply), manifest reference, worked example using `FileRef` in dev and `K8sSecretRef` in cluster. _(Canonical reference created at `docs/tenant-provisioning.md`, grounded in the real `TenantManifest` shape from `cli/src/server/tenant_api.rs` — `repository` / `config.fields` / `config.secretReferences` / `secrets[]` / `providers`. Covers bootstrap, day-2 render→diff→apply loop, full manifest reference with every field, subcommand catalogue, and a "Under the hood" section on the `POST /provision` contract.)_
- [x] 14.2 Update CLI reference docs for new subcommands. _(Rewrote the "Tenant Control Plane" section of `docs/guides/cli-quick-reference.md` around the manifest pipeline: new "Tenant manifest pipeline (all mutations)" table with `render`/`validate`/`diff`/`apply`/`watch`; reads separated; non-manifest lifecycle (`deactivate`, `use`, `switch`, `current`) called out; connection visibility explicitly kept as a standalone surface per §7.6 follow-up.)_
- [x] 14.3 Security appendix: secret input modes, token scopes, readiness contract. _(New `docs/security/tenant-provisioning-security.md`. §1 distinguishes the two sensitive channels (`secrets[]` bound bytes vs `config.secretReferences{}`), enumerates the four reference kinds with per-env gates, and states the `SecretBytes` / no-`Serialize` / no-`Clone` plaintext invariants. §2 documents the scope vocabulary (`tenants:read|provision|validate|diff|watch`, `connections:manage`), the mint→use→revoke lifecycle, and the user-OR-service principal model. §3 makes the readiness contract explicit: what `apply` guarantees, what it does not, the SSE event order, and what is deliberately excluded (indexing completion, downstream caches, session role propagation).)_
- [x] 14.4 Migration guide: moving from fine-grained subcommands to `render` + `apply`. _(Shipped in the §7.6 PR as `docs/guides/tenant-cli-migration-v0.9.md`. Corrected in the §7.6 follow-up: field names now match the real manifest (`repository`, `config.fields`, `secrets[].secretValue`, no `connections[]` field), and a new "Preserved (no manifest equivalent)" section documents why `connection grant/revoke` and `deactivate` are intentionally NOT migrated.)_
